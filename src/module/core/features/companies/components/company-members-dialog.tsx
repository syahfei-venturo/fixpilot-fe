// components/company-members-dialog.tsx (stub, replaced in Task 6)
import type { Company } from '../types';

type Props = { open: boolean; company: Company | null; onClose: () => void };

export function CompanyMembersDialog(_props: Props) {
  return null;
}
